# C++：std::greater()、std::less()、自定义比较函数的规则
`比较第一个元素是否比第二个元素大或者小。`
排序和建堆的效果
排序：
`less<T>`升序排列，需要实现 opertor< 。比较两个值，如果第一个值小于第二个值，则返回 `true`，否则返回 `false`。
`greater<T>`降序排列，需要实现 opertor> 。比较两个值，如果第一个值大于第二个值，则返回 `true`，否则返回 `false`。



